#!/usr/bin/expect
#set username ""
#set password ""


set timeout 15
eval spawn git [ join $argv ]

expect {
  "Username: " {
    send "$username\n"
    expect "Password:"
    send "$password\n"
    interact
  }
}